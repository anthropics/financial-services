# ABOUTME: IBKR MCP server package — exposes Interactive Brokers TWS / Gateway data
# ABOUTME: to Claude Code / OMP as MCP tools. Read-only research surface.

"""IBKR MCP server.

Exposes a focused, read-only research surface over Interactive Brokers TWS
or IB Gateway via the `ib_async` library. Designed to be installed only in
this repository (see the repo-root `.mcp.json`).
"""

__version__ = "0.1.0"
