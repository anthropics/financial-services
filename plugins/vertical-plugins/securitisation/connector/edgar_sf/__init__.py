"""edgar_sf — a free, local MCP connector for US SEC EDGAR structured-finance data.

US-first, but organised so EU and Australia can be added as parallel regional
modules later without changing the tools or the shared core.
"""

__version__ = "0.1.0"
